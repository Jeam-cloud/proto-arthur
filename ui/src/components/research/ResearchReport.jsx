// REPLACED by ResearchPaper.jsx.
//
// Research mode used to end in a short block-structured "report". It now
// produces a full literature review with an abstract, headed sections,
// style-formatted citations and a reference list -- see ResearchPaper.jsx.
//
// This file is intentionally left as a stub rather than silently deleted so
// that a stale import anywhere fails loudly with a clear reason instead of
// resolving to a blank screen. Delete it once you have confirmed nothing
// imports it.
export default function ResearchReport() {
  throw new Error(
    "ResearchReport was replaced by ResearchPaper. Import ResearchPaper instead.",
  );
}
